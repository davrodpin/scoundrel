# Environment
NODE_ENV=production

# Port (usually set by the hosting platform)
PORT=3001

# CORS origins (comma-separated)
# In production, this should only include the frontend deployment URL
ALLOWED_ORIGINS=https://davrodpin.github.io 